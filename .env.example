# No server-side API keys needed!
# Users enter their own API keys in the app UI.
# Keys are stored in their browser's localStorage only.

# Optional: Set a default API key for development
# NEXT_PUBLIC_DEFAULT_PROVIDER=gemini
